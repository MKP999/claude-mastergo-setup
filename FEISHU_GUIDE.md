# MasterGo MCP for Claude Code - 安装使用指南

> 一键配置 MasterGo MCP，让 Claude Code 直接读取 MasterGo 设计稿并生成项目代码

---

## 📋 目录

1. [项目介绍](#项目介绍)
2. [快速安装](#快速安装)
3. [安装验证](#安装验证)
4. [使用方法](#使用方法)
5. [常见问题](#常见问题)
6. [更新日志](#更新日志)

---

## 项目介绍

### 核心功能

| 功能 | 说明 |
|------|------|
| 🔌 **一键安装** | 自动注册 MasterGo MCP 服务 |
| 📦 **内置 Skill** | 完整的设计到代码转换工作流 |
| 🎯 **智能识别** | 自动识别项目框架（Vue 2/3、React 等） |
| 🏢 **内部镜像** | 支持公司内部 MasterGo 镜像 |
| 🔄 **1:1 还原** | 保持设计与代码像素级一致性 |

### 支持的框架

- Vue 2 + Element UI
- Vue 3 + Element Plus
- React + Ant Design
- 公司内部组件库（dst-v3-ele-components 等）

---

## 快速安装

### 前置要求

- Node.js >= 18
- Claude Code CLI（`npm install -g @anthropic-ai/claude-code`）
- MasterGo API Token

### 获取 Token

1. 登录 [MasterGo](https://mastergo.com)
2. 进入 **个人设置** → **API Token**
3. 创建并复制你的 Token

### 安装步骤

#### 方式一：全局安装（推荐）

```bash
# 1. 克隆项目
git clone https://github.com/MKP999/claude-mastergo-setup.git
cd claude-mastergo-setup

# 2. 一键安装
./setup.sh --token=<your_mastergo_token>
```

#### 方式二：使用公司内部镜像

```bash
./setup.sh --token=<your_token> --url=https://your-internal-mastergo.com
```

#### 方式三：项目级安装

```bash
cd /path/to/your-project
/path/to/claude-mastergo-setup/setup.sh --token=<your_token> --scope=project
```

### 安装参数说明

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--token` | MasterGo API Token（必填） | - |
| `--url` | MasterGo 服务地址 | `https://mastergo.com` |
| `--scope` | 安装范围：`user`(全局) 或 `project`(项目级) | `user` |
| `--no-skill` | 跳过 Skill 文件安装 | - |
| `--help` | 显示帮助信息 | - |

---

## 安装验证

### 1. 检查 MCP 服务

```bash
claude mcp list
```

**预期输出：**
```
Checking MCP server health...

mastergo-magic-mcp: npx -y @mastergo/magic-mcp ... - ✓ Connected
```

### 2. 检查 Skill 文件

```bash
# 全局安装
ls ~/.claude/skills/mastergo-implement-design.md

# 项目级安装
ls .claude/skills/mastergo-implement-design.md
```

### 3. 查看 MCP 配置

```bash
claude mcp show mastergo-magic-mcp
```

---

## 使用方法

### 基本使用

#### 1. 粘贴 MasterGo 链接

直接在 Claude Code 中粘贴设计稿链接：

```
https://mastergo.com/design/xxxxx?layer_id=xx-xx
```

#### 2. 直接描述需求

```
帮我实现这个 MasterGo 设计
```

### 工作流程

```
┌─────────────────┐
│  1. 解析 URL     │
│  提取设计稿信息  │
└────────┬────────┘
         ▼
┌─────────────────┐
│  2. 识别框架     │
│  检查项目技术栈  │
└────────┬────────┘
         ▼
┌─────────────────┐
│  3. 获取设计数据 │
│  调用 mcp__getDsl│
└────────┬────────┘
         ▼
┌─────────────────┐
│  4. 生成代码     │
│  按框架转换      │
└────────┬────────┘
         ▼
┌─────────────────┐
│  5. 验证还原     │
│  1:1 视觉对比    │
└─────────────────┘
```

### 框架识别规则

| 检测项 | 框架判断 |
|--------|----------|
| `vue: "2.x"` | Vue 2 |
| `vue: "3.x"` | Vue 3 |
| `react` | React |
| `element-ui` | Element UI (Vue 2) |
| `element-plus` | Element Plus (Vue 3) |
| `antd` | Ant Design (React) |
| `dst-v3-ele-components` | 公司组件库 |

### 代码示例

**Vue 3 + Element Plus**
```vue
<template>
  <el-button type="primary">按钮</el-button>
</template>

<script setup>
// 使用 Composition API
</script>
```

**Vue 2 + Element UI**
```vue
<template>
  <el-button type="primary">按钮</el-button>
</template>

<script>
export default {
  // 使用 Options API
}
</script>
```

---

## 常见问题

### Q1: 安装后 Claude 没有反应？

**A:** 重启 Claude Code 或重新打开会话，确保 MCP 服务已加载。

### Q2: 如何验证安装是否成功？

**A:** 运行 `claude mcp list`，查看是否包含 `mastergo-magic-mcp`。

### Q3: 生成了 HTML 文件而不是组件？

**A:**
1. 确保项目根目录有 `package.json`
2. Skill 会自动识别框架并生成对应代码
3. 如果仍有问题，手动告知框架：`这是一个 Vue 3 项目`

### Q4: 项目级和全局安装有什么区别？

**A:**
| 类型 | 范围 | 适用场景 |
|------|------|----------|
| 全局安装 | 所有项目 | 推荐，一次配置 |
| 项目级安装 | 当前项目 | 需要独立配置 |

### Q5: 如何卸载？

```bash
# 卸载全局配置
./uninstall.sh

# 卸载项目级配置
./uninstall.sh project
```

### Q6: Token 在哪里存储？

**A:** Token 存储在 Claude Code 的 MCP 配置中，位置：
- 全局：`~/.claude/settings.json`
- 项目：`.claude/settings.json`

### Q7: 支持哪些 MasterGo 工具？

| 工具 | 说明 |
|------|------|
| `mcp__getDsl` | 获取设计稿 DSL 数据 |
| `mcp__getComponentLink` | 获取组件文档链接 |
| `mcp__getMeta` | 获取页面元信息 |
| `mcp__getComponentGenerator` | 启动组件开发工作流 |

---

## 更新日志

### v1.1.0 (2025-02-12)

- ✨ 新增框架识别功能
- ✨ 新增按框架生成代码
- ✨ 新增公司内部组件库支持
- 🐛 修复生成 HTML 文件的问题
- 📝 完善文档和使用说明

### v1.0.0 (2025-02-12)

- 🎉 首次发布
- ✨ 一键安装 MCP 服务
- ✨ 内置 Skill 工作流
- ✨ 支持内部镜像

---

## 相关链接

- **GitHub**: https://github.com/MKP999/claude-mastergo-setup
- **MasterGo 官网**: https://mastergo.com
- **Claude Code 文档**: https://docs.anthropic.com/en/docs/claude-code
- **MasterGo MCP**: https://github.com/mastergo/magic-mcp

---

*如有问题或建议，欢迎提 Issue 或 PR*
